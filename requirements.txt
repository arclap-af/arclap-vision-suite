# Pipeline / ML
ultralytics==8.4.41
yt-dlp>=2024.4.0           # YouTube / Vimeo URL ingest
inference-sdk>=0.9         # Roboflow hosted-workflow detection
opencv-python>=4.10
numpy>=2.0
tqdm>=4.66
pillow>=10.0                # CLIP refinement reads images via PIL
open-clip-torch>=2.24       # CLIP zero-shot refinement (Filter Section D)
ffmpeg-python>=0.2          # Python wrapper used by the timelapse video render
duckduckgo-search>=6.0      # Web image collection in the Swiss Detector tab
requests>=2.31              # HTTP client for image downloads + Roboflow workflow calls

# FastAPI web app (preferred UI: app.py)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.20
pydantic>=2.7

# Gradio web app (alternative UI: gui.py)
gradio>=5.0
matplotlib>=3.9

# NOTE: PyTorch must be installed separately so the right CUDA build is selected.
# See README.md.  For NVIDIA GPUs (CUDA 12.4):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# For CPU-only:
#   pip install torch torchvision
#
# The ffmpeg BINARY (not the Python wrapper) is also required for video render.
# install.bat / install.sh handle this automatically; manual fallback:
#   winget install -e --id Gyan.FFmpeg          (Windows)
#   brew install ffmpeg                         (macOS)
#   sudo apt install -y ffmpeg                  (Debian/Ubuntu)
